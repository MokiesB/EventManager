package dk.MMJT.DAL.DAO;

import dk.MMJT.BE.User;

public class UserDAO {
    public User loginUser(String username, String password) {
        User user = null;
        //DB quary here
        //get user from db and check if user.type = "Admin"
        // if admin user = new Admin(data here)
        // if (user.type = "EventManager") user = new EventManager();
        // else customer
        return user;
    }
}
